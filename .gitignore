# Never publish the API credentials
config.ini
.ec5_token.json

# Local working output (originals, tables, geopackage)
output/
*.gpkg

# Auto-downloaded face detector weights (~230 KB, fetched on first run)
models/
